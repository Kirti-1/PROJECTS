// insertion of an element in already sorted array
import java.util.*;
public class InsertionSort{
    public static void main(String[] args){
        // Scanner scn = new Scanner(System.in);
        // // int n = scn.nextInt();
        // int n = 10;
        // int []arr = {11,23,43,46,56,100,136,138,180,0};
        // int val = scn.nextInt();
        // int i=n-2;
        // while(i>=0 && arr[i]>val){
        //         arr[i+1] = arr[i];
        //         i--;
        //     }
        // arr[i+1] = val;            
        // for(int l=0;l<n;l++){
        //     System.out.format("%d ",arr[l]);
        // }
        // Throwable tr = new Throwable("Hello");
        Exception tr = new Exception("helloooooooooo");
        System.out.println((new Throwable("hyy")).getMessage());
        System.out.println((new ClassNotFoundException()).getMessage());
        System.out.println(tr.getMessage());

    }
}