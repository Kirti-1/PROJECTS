#include <stdio.h>
// #define SIZE 100
// int a;
// int a=10;
// int a;
// int a;
void main(){
   // int a = 10;
 /* \
   4 parts - 
    code - text part
    global - only global variables are stored (default value: 0) 
    stack - all loacal variables and function calls are stored (default value - garbage)
    heap - 
*/
   // anything located at stack gets destroyed atomatically it doesn't need to get explicitly destroyed by us.
   // global variables can be declared any number of time but in reality it is considered ones
   // printf("%d",a); // can be declared any number of time but can't be re -defined
   // int i=0;
   // while(i<10)
   // {
   //    // int c = (a == 10)? break : continue; BREAK IS STATEMENT AND NOT AN EXPRESSION!!
   //    i++;
   // }
   // char name[SIZE];
   //  int x;
   //  int i;

   //  printf("Enter your name: ");

   //  scanf("%s", &name);

   //  for (i = 1; name[i] != '\0'; i++);
   //      printf("Are you sure your name is: ");

   //  for (x = i - 1; x >= 0; x--)
   //      printf("%c", name[x]);
   // int a,b,c;
   // scanf("%d%d%d",&a,&b,&c);
   // int min = a;
   // if(min>b){
   //    min = b;
   // }
   // if(min>c){
   //    min = c;
   // }
   // printf("Mininmum out of three = %d",min);
   // int a,b,c = 1,2,3; can't assign like this!!
   // int a = 10,b = 12,c =12;
   // int a = -4; // comma operator executes left to right.
   // printf("%d",a);
   // if(a++,++a,a++,++a){ // pre return value 0 false
   //    printf("ede\n"); // only last value returned to check condition.
   // }else{                 // undefined behaviour
   //    printf("edesddd\n");
   // }
   // a = -4;
   // if(a++,++a,a++,a++){ // post return value -1 true
   //    printf("ede");
   // }else{
   //    printf("edesddd");
   // }
   // int result;
   // 20>10?result=10:result=1; // ERROR!!
   // 20>10?(result=10):(result=1); //correct format
   // 20>10?(return 0):(return 1); //expected an expression and not a statement

       

 
}