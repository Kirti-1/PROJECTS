1. Virtual Function can't be static - as static Function is a property of class and not an object thus it can only be accessed using a class name with scope resolution operator.
Virtual Functions are re-defined(overriden) by a derived class and static functions can't be overriden as virtual function are bind by 
static binding & dynamic binding
